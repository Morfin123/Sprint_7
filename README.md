# Проект автоматизации тестирования API cервиса «Яндекс.Самокат»
1. API автотесты
2. Команда для установки всех зависимостей — pip3 install -r requirements.txt
3. Команда для запуска тестов — pytest -v